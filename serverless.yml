service: serverless-example

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs20.x

functions:
  main:
    handler: build/main.handler
    events:
      - http:
          method: ANY
          path: /
      - http:
          method: GET
          path: /days
